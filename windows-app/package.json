{
  "name": "markdown-studio",
  "version": "1.1.0",
  "description": "A lightweight Markdown and plain-text editor and previewer",
  "main": "main.js",
  "license": "MIT",
  "author": "Markdown Studio contributors",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js",
    "dist:win": "electron-builder --win nsis portable"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "local.markdownstudio.app",
    "productName": "Markdown Studio",
    "files": [
      "main.js",
      "preload.js",
      "renderer/**/*"
    ],
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "artifactName": "Markdown-Studio-${version}-Windows-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true
    },
    "fileAssociations": [
      {
        "ext": [
          "md",
          "markdown",
          "txt",
          "text"
        ],
        "name": "Markdown and Text Document",
        "role": "Editor"
      }
    ]
  }
}
